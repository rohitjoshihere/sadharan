import FeaturedProducts from '../components/FeaturedProducts';

const Shop = () => {
    return (
        <main style={{ paddingTop: '5rem' }}>
            <FeaturedProducts />
        </main>
    );
};

export default Shop;
