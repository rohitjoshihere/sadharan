import LaunchPreview from '../components/LaunchPreview';

const Shop = () => {
    return (
        <main style={{ paddingTop: '5rem' }}>
            <LaunchPreview />
        </main>
    );
};

export default Shop;
